Synthetic software planning exercise. No real persons, repository, business data or external facts.
An application has a two-party agreement draft. Each party approves the current version and canonical material hash. Material edits invalidate approvals. Only the authenticated party may change its own approval. Expiry and revocation block introduction. Missing feedback is counted separately. Workers only propose artifacts; an independent local checker accepts. A worker can propose smaller tasks but cannot dispatch them or increase budget.
Return JSON with exactly one key cases, an array of 5 distinct adversarial test specifications. Each case must have id, setup, action, expected, invariant. Cover material-version races, forged other-party approval, expired consent, missing feedback denominator and recursive-worker budget escape. Use short complete strings. Describe concrete observable behavior and avoid implementation-specific assumptions. These will be independently checked and adapted into a future execution plan. No markdown and no code execution.
