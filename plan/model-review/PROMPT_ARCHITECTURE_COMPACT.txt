Review this synthetic architecture contract in at most 900 words. Output only a numbered list of up to 8 material findings. Each item: `severity | failure scenario | smallest correction | semantic proof`. End with exactly 3 mutation tests and 3 proposals to reject. No praise, no invented code, laws, facts, prices, or providers.

Contract: one deterministic matcher and one minimal-profile bridge produce a non-binding business case for two solo professionals. Flow is declared need/contribution -> explicit comparison consent -> candidate -> pair-specific case disclosure consent -> versioned material terms -> both parties approve the same case/version/hash -> introduction request -> trial -> recipient accepts the specific outcome. Permissions stay separate: visibility, comparison, case disclosure, terms approval, introduction, external AI, recording, summary use, communications. Material edit, expiry, revocation, or consent withdrawal invalidates the relevant next action. Contact and sensitive/untrusted fields cannot affect matching or be disclosed early.

Modes: exchange is bilateral complementary value; paid_service requires supplier coverage plus explicit paid-mode intent from both while compensation remains unresolved until approved; referral requires declared source, intended recipient, benefit, third-party uncertainty, and compensation status; hybrid requires every named leg to pass independently; legacy joint_project remains compatible but parked. Missing new fields means needs_information, not legacy corruption.

Order: freeze semantic fixtures -> extend existing matcher -> bridge profile -> upgrade existing case compiler -> pure canonical material payload/version/hash/two-current-approvals/expiry/revocation/change invalidation -> canonical SQL-source proposal and generated outputs -> local synthetic pair workflow/metrics -> independent diff review, deliberate mutations, full tests, offline build. No duplicate matcher/router/ledger. Generated SQL is not hand-edited.

D1 proves offline behavior only. D2 needs disposable-database RLS attacks. D3 needs two authorized people and fresh remote evidence. D4 needs a fixed real cohort. D5 is commercial launch. Overnight target D1; no deployment, migration, outreach, messaging, payment, recording, or live-user action.

Execution: max 3 active including integrator, one writer per file, exact dependencies/files/hashes/invariant/negative cases/test/rollback/marker per task, independent reviewer cannot accept own work. Provider output requires exact receipt and separate local acceptance; failure, unknown cost, truncation, or privacy rejection stops the lane.
