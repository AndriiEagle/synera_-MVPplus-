# Математика A/B/C: що з цього реально працює

Витяг з трьох Astra-промптів (хеші звірені з `manifest.json`, 4/4 збіг) плюс з усього, що сталося в цій сесії. Кожне твердження нижче перевірене брутфорсом або аналітично — код прогнаний, одне моє твердження при цьому **провалилось і виправлене** (розділ 10).

---

## 1. Три стратегії — це одна формула з одним параметром

Arm C дає загальну форму:

```
F(z) = bᵀz + ½ zᵀMz − λ‖z‖₀ ,   z ∈ {0,1}ⁿ ,   M = S − C  (симетрична, нульова діагональ)
```

- `b_i` — сама-по-собі користь виконавця i
- `M_ij` — взаємодія пари (i,j): синергія мінус конфлікт
- `λ` — ціна складності/координації за кожного доданого учасника
- `‖z‖₀` — скільки учасників увімкнено

Перевірено кодом:

| Arm | Обмеження | Що стає з формулою |
|---|---|---|
| **A** Інженер | `‖z‖₀ = 1` | Квадратичний член **тотожно зникає** → `F = b_i − λ`. Це дослівно A-шний `argmax[LCB(ΔJ) − κ·Complexity]` |
| **B** Дослідниця | `‖z‖₀ ≤ 2` | Рівно **один** член `M_ij`. B-шний `β·NovelCoverage` — це і є той слот |
| **C** Архітекторка | `‖z‖₀ ≤ 3` | Повна форма |

**Отже A ⊂ B ⊂ C. Це не три стратегії — це один алгоритм з трьома значеннями k = ‖z‖₀.** Турнір «хто переможе» математично безпредметний: питання не «яка стратегія», а «яке k».

Додатковий факт із діффу файлів: A/B/C відрізняються рівно ~14 рядками зверху, решта ~340 рядків «Спільного незмінного контракту» — байт-у-байт однакові. Формульний збіг вище це підтверджує структурно.

---

## 2. Симетрія, яку ти просив

`M = S − C` симетрична за побудовою: синергія i з j дорівнює синергії j з i. Це не естетика, це має наслідок — уся інформація про «хто з ким працює» лежить у `n(n−1)/2` числах верхнього трикутника. Не в `n²`, не в історіях, не в назвах моделей. У **45 числах для 10 виконавців**.

Красиве тут те, що обидві матриці — синергія і конфлікт — входять з протилежним знаком у **ту саму** квадратичну форму. Тобто «допомагають» і «заважають» — не дві різні підсистеми, а одне число з різним знаком. Жодної додаткової машинерії не треба.

---

## 3. Точний критерій «коли пара краща за одинака»

Перевірено на 15 парах, 0 розбіжностей з брутфорсом:

```
пара (i,j) б'є обох одинаків   ⟺   M_ij  >  λ − min(b_i, b_j)
```

Словами: **синергія має перевищити ціну координації мінус користь слабшого учасника.** Слабкий виконавець виправданий у парі тільки якщо синергія покриває і його слабкість, і накладні витрати.

---

## 4. Справжня структура: три режими, задані відношенням λ/b

Це головне. A проти C — це не змагання стратегій, це **режим**, у якому ти зараз перебуваєш:

| Режим | Умова | Оптимальне k | Що робити |
|---|---|---|---|
| Координація дешева | `λ ≪ b` | k = 3 (впирається в кеп) | Бери всіх, синергія майже не важить |
| Проміжний | `λ ≈ b` | k = 2 | **Тільки тут матриця M щось вирішує** — тільки тут її варто міряти |
| Координація дорога | `λ ≫ b` | k = 1 (або 0) | Arm A виграє за побудовою, без експерименту |

Прогін по λ від 0.1 до 2.1 при `b̄ ≈ 1.1` дав рівно цей перехід: k=3 → k=2 → k=0.

**Наслідок, який економить тобі 12 годин:** якщо ти вимірюєш λ/b і потрапляєш у крайній режим — порівняльний пілот не потрібен взагалі, відповідь уже визначена. Мірити M треба **лише в проміжному режимі**.

---

## 5. Де ти зараз сидиш — за реально виміряними даними

Єдиний прогін, що справді відбувся (Arm C, within-C ablation):

| Сім'я | det (одинак) | pair | Δ |
|---|---|---|---|
| f1 json_repair | 1.00 | 1.00 | 0.00 |
| f2 log_mining | 1.00 | 0.00 | −1.00 |
| f3 numeric_summary | 1.00 | 0.50 | −0.50 |
| **середнє** | **1.000** | **0.500** | **−0.500** |

`M_ij − λ = −0.500` на кожній сім'ї. Координація перевищила синергію **скрізь**. Це режим `λ ≫ b` → **k = 1 → Arm A, на виміряних доказах**, а не за вподобанням.

**Але:** це λ за умов, коли субагенти вмирали від вичерпаної GLM-5.3 квоти й обривів сеансу. Тобто ти виміряв **ціну координації зі зламаною координацією**. Полагодь квоту й стабільність — λ впаде, і режим може змінитись. Число −0.500 чесне, але воно про твою інфраструктуру, не про формулу C.

---

## 6. Чому твій власний ліміт «3 воркери» врятував усю задачу

Загальна `F(z)` на бінарних z з `‖z‖₀`-штрафом — це UBQP, **NP-складна**. Але з кепом k≤3:

| n виконавців | 2ⁿ підмножин | k≤3 підмножин | Скорочення |
|---|---|---|---|
| 10 | 1 024 | 175 | 6× |
| 20 | 1 048 576 | 1 350 | 777× |
| 50 | 1.1 × 10¹⁵ | 20 875 | 5.4 × 10¹⁰× |

**Правило «максимум 3 активні воркери» — це не лише безпека. Це те, що робить оптимізацію точно розв'язною брутфорсом за мікросекунди.** Ніякого оптимізатора, ніякої евристики, ніякого «розумного» пошуку. Перебір 175 варіантів.

---

## 7. Чому Arm A правильно йде першою (Amdahl)

Прискорення від покращення компонента i у s_i разів: `S = 1/Σ(p_j/s_j)`, де p_j — частка часу.

Перевірено чисельно: `∂S/∂s_i |_{s=1} = p_i`, розбіжність 2.5×10⁻⁷.

**Граничний виграш від оптимізації компонента дорівнює рівно його частці wall time.** Тому A-шне «розклади час, знайди домінантний внесок, ріж його» — не «консервативний вибір», а **доказово найкрутіший градієнт**. Будь-яка інша перша дія має менший нахил. Це аргумент за A, якого в самому промпті немає.

---

## 8. Чому 100 роботів на NVIDIA нічого не дадуть (Little)

`L = λW` → `λ = L/W`. Твій L зафіксований на 3 твоїм же правилом.

| Середня задача W | Пропускна здатність λ |
|---|---|
| 30 хв | 6 задач/год |
| 15 хв | 12 задач/год |
| 7.5 хв | 24 задач/год |

**При L=3 пропускна здатність залежить ВИКЛЮЧНО від W.** Сто доступних роботів не змінюють нічого — вони не можуть бути активними. Єдиний важіль — коротші задачі. Це математична відповідь на твій позавчорашній запит, замість мого тодішнього просто «ні».

---

## 9. Три реальні баги у формулах, які варто полагодити

1. **U(Θ) має зв'язані одиниці.** `J` визначений як користь **за одиницю wall time** (швидкість), а `C_full` — це витрати (запас). Щоб їх додати, `μ` мусить мати одиниці 1/час. Отже **μ не вільна вага — вона прив'язана до вікна вимірювання.** Змінив пілот з 30 хв на 12 год — μ мовчки переваживсь, і два прогони стали незрівнянними. Треба або нормувати C_full на час, або фіксувати μ на кожне вікно окремо.

2. **CVaR у першому пілоті принципово необчислюваний.** При 12 cases × 3 повтори = 36 проб: хвіст на α=0.05 містить 1.8 спостереження, на α=0.20 — 7.2. Для стабільного середнього хвоста треба ≳20. Тобто `U(Θ)` **не може бути порахована в першому пілоті за побудовою** — і README це вже правильно каже («CVaR=unknown»). Варто зафіксувати явно: U — метрика другої фази, primary завжди accepted-value/hour.

3. **V = eᵀPe — кандидат, не доказ.** Промпт це чесно хеджує. Додам корисне: тобі не потрібна повна теорія Ляпунова. Тобі потрібна **дискретна перевірка спадання**: логуй e_t на кожному 30-хв чекпойнті й дивись, чи `V(e_{t+1}) < V(e_t)` виконується на більшості кроків. Це виміряна швидкість стиснення, не доказ стійкості — але воно **дієве**: якщо V не спадає по чекпойнтах, цикл не самостабілізується, зупиняй.

---

## 10. Моя власна помилка в цьому ж аналізі

Я спершу заявила: «якщо M від'ємно напіввизначена, то k=1 оптимальне». **Брутфорс це спростував** — при чисто конфліктній M оптимум усе одно вийшов k=3, бо кожне `b_j` значно перевищувало λ, і воркери окупались попри конфлікт.

Правильне правило (перевірене на 60 значеннях λ, 0 розбіжностей):

```
Δ_j(S) = b_j + Σ_{i∈S} M_ij − λ        додавати j iff Δ_j(S) > 0
```

k=1 оптимальне **не** через знак M, а тоді, коли жоден другий виконавець не перебиває планку проти чинного. Залишаю це тут як приклад, а не прибираю — саме так мають виглядати виправлені твердження.

---

## 11. Експеримент, який реально вирішує питання (замість турніру)

Не треба 3 arms × 12 cases × 3 повтори = 108 прогонів плюс sealed holdout.

Треба: `n` соло-замірів + `C(n,2)` парних → повна оцінка M.

| n виконавців | Проб треба | Проти турніру (108) |
|---|---|---|
| 6 | 21 | 5.1× дешевше |
| 10 | 55 | 2.0× дешевше |
| 15 | 120 | приблизно рівно |

І головне — **це міряє M напряму, а не вгадує переможця непрямо.** З M на руках ти рахуєш оптимальне k точно, для будь-якого λ, назавжди, без повторних турнірів.

Порядок дій, який випливає з усього вище:

1. Полагодь квоту/стабільність сеансів — інакше міряєш λ зламаної системи (розділ 5).
2. Виміряй `b_i` соло по кожному виконавцю на спільному наборі задач.
3. Порахуй `λ/b̄`. Крайній режим → відповідь готова, стоп. Проміжний → крок 4.
4. Виміряй `C(n,2)` пар → M. Брутфорс 175 підмножин → оптимальне k і склад.
5. Amdahl-розклад часу (розділ 7) виконуй **паралельно і першим** — він має найбільший градієнт незалежно від усього іншого.

---

## Межі цього документа

Вся математика перевірена на **синтетичних** даних і аналітично. Жодне число тут не походить з твоєї реальної системи, крім таблиці розділу 5, яка взята з Arm C-звіту (n=3 сім'ї, один прогін, напарник загинув посеред прогону — оцінка дуже слабка). `b_i`, `M_ij`, `λ` для твого флоту **ще не виміряні**. Формули коректні; входів до них немає.

Provider calls: 0. $0.00.
