# Synera wave C status — PRESENT_BUT_UNTESTED

<!-- SYN_SQL_LABELLED_UNTESTED -->

Date: 2026-09-11. Label: **`PRESENT_BUT_UNTESTED`**.

## What exists

| File | Role |
|---|---|
| `supabase/case-state.proposal.sql` | Additive migration: brief v2 validator (C0), `match_cases` (C1), `match_case_approvals` (C2), server-side introduction gate on `meeting_requests` |
| `supabase/case-state.acceptance.sql` | Transactional RLS acceptance with fixtures, always `ROLLBACK` (C3) |
| `neon/case-state.migration.sql`, `neon/case-state.acceptance.sql` | Generated by `neon/generate-schema.mjs`; never edited by hand (C4) |

## What is proven, and what is not

- Node tests (`neon/worker.test.mjs`) prove **generation only**: the Neon files are byte-reproducible from the Supabase sources, carry no `auth.users`/`auth.uid()` leftovers, add the members-only gate to both new tables, and leave the already-applied `neon/schema.proposal.sql` byte-identical.
- Node tests **never** prove RLS behaviour, and no statement in these files has been run against any database. Not even the syntax has been parsed by Postgres.
- The server does not recompute the canonical SHA-256 of `material`. Clients check integrity (`stateIntegrityProblems` in `business-case.mjs`) before any next step.

## Why a separate migration

The live Neon project already runs `neon/schema.proposal.sql` (`docs/NEON_LAUNCH.uk.md`: 106 statements, acceptance PASS). That source is frozen history. Editing `supabase/real-pilot.proposal.sql` would make the repository disagree with the live database, so everything new goes into `case-state.proposal.sql`.

## What must happen together before any apply

1. `case-state.acceptance.sql` passes on a **disposable** database (Neon branch or local Docker Postgres). Needs a separate human approval (H1).
2. The client writes cases and approvals in the new shape: `saveCaseState` still posts one JSON `state` blob, which would let a client write the other party's approval (step B2-rework).
3. `neon/worker.mjs` allows `match_cases` and `match_case_approvals` (its `TABLES` set has six tables today).
4. The UI from wave A is deployed together with the migration. Without it the live database rejects brief v2 (pilot modes). And after it, `meetings_need_approved_case` refuses every invitation that has no case approved by both parties.

The label is removed only after item 1, with the run receipt attached here. There is no production migration in this plan.
